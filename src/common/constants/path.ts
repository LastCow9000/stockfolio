import { join } from 'path';

export const VIDEO_PATH = join(process.cwd(), 'public');

export const ORIGINAL_VIDEO_PATH = join(VIDEO_PATH, 'origin-videos');

export const PUBLIC_ORIGINAL_VIDEO_PATH = join('public', 'origin-videos');
